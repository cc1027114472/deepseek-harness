import type { ReactNode } from 'react'
import type { SessionLogDownloadDialogProps } from './Dialog.tsx'

/**
 * Render the Session Header export capsule and its shared result dialog.
 * @param _props - Session runtime, download controller, and localized dialog copy.
 * @returns the persistent Header action and Session-scoped dialog.
 */
export function SessionLogDownloadHeaderAction(_props: SessionLogDownloadDialogProps): ReactNode {
  return null
}
